name: CI
on:
  push:
  workflow_dispatch:
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - run: |
          gh pr comment 1 --body "Hi from GitHub CLI"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # - uses: actions/checkout@v3
      # - uses: ruby/setup-ruby@v1
      #   with:
      #     ruby-version: 3.1
      # - run: gem install gem-compare --version 1.1.0
      # - run: gem compare pg 1.4.0 1.4.1
      # - run: gem compare --diff pg 1.4.0 1.4.1
